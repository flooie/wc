# PyScript configuration.
# See https://docs.pyscript.net/latest/user-guide/configuration/
name = "World Cup Scores (PyScript)"
description = "FIFA World Cup scores from the ESPN API, rendered in Python via PyScript."

# No third-party packages needed for these demos — everything uses the
# Python standard library, which keeps the page fast to load.
# To add packages from PyPI, list them here, e.g.:
# packages = ["numpy", "matplotlib"]
packages = []
